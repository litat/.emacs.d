;; 
;; Some handy features that built-in emacs.
;; 

(whitespace-mode)
(glasses-mode)
(subword-mode)
(icomplete-mode)
(show-paren-mode)
(electric-pair-mode)
(delete-trailing-whitespace)
(narrow-to-defun)

;; Features that needs parameters.
(ffap)
(flush-lines)
